To run many of these examples, use node [scriptname].

Scripts that depend on libs require that these libs be installed prior.

  for example, if immutable is required, make sure to to npm install immutable

