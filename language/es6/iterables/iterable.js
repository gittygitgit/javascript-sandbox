
// @@iterator

// Symbol.iterator

/*
    An iterable object

    Object which has an @@iterator method

*/

/* 
    Making an object iterable

    An object is iterable if it has a method named Symbol.iterator that returns an iterator

    Assign a method to @@iterator

    Has a method named @@iterator

    @@iterator method returns an iterator
*/

/*
    How to assign a method to @@iterator

    well-known Symbol.iterator symbol

    
*/

/* 
    @@iterator

    invoked once before an object is to be iterate
    Assign a method to @@iterator

    An iterator named Symbol.iterator that returns an iterator

*/

/*
    Iterator interface

    has a next() method that returns an IteratorResult

    IteratorResult contains methods value and done
*/ 
    
