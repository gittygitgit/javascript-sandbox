const { Seq } = require('immutable');

// Seq is a subclass of Collection

// Why?
// A lazy implementation.  Does the least amount of work required.  
// Lets you specify various chained collection methods without being expensive.

